# LangChain 핵심
# 0.3 라인으로 상한 고정: 1.x는 RunnableWithMessageHistory 등 API가 변경됨
langchain>=0.3.30,<1.0
langchain-openai>=0.3.35,<1.0
langchain-chroma>=0.2.0,<1.0
langchain-community>=0.3.0,<1.0

# 유틸리티
python-dotenv>=1.0.0
pydantic>=2.0.0
redis>=5.0.0
# structured outputs(json_schema) 지원을 위해 2.x 필요
openai>=2.0.0

# 문서 처리 (전처리 시에만 필요)
pypdf>=3.0.0
langchain-text-splitters>=0.3.0,<1.0

# fastapi 추가 의존성 설치
fastapi[standard]
