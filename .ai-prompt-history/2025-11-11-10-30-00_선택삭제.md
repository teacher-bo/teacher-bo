# process_rulebooks.py 선택적 파일 삭제

- 날짜: 2025-11-11 10:30:00
- 작업: 선택삭제

## 요구사항

- `clean_directories` 함수가 모든 파일을 삭제하는 대신, `get_game_names_from_pdfs`로 찾은 게임에 해당하는 파일만 선택적으로 삭제.

## 적용 내용

- `rag-vector-db-generator/process_rulebooks.py`
  - `get_game_names_from_pdfs` 함수를 파일 상단으로 이동(clean_directories보다 먼저 정의).
  - `clean_directories(game_names: dict)` 시그니처 변경: game_names 딕셔너리를 인자로 받음.
  - 디렉터리 전체 삭제 대신, 각 게임명에 해당하는 파일만 개별 삭제(.rulebook_text.txt, .rulebook.txt).
  - `main` 함수에서 호출 순서 조정: PDF 스캔 → 선택적 파일 삭제 → 처리.

## 품질 게이트

- Build: PASS (구문 오류 없음)
- Lint/Typecheck: PASS (타입 힌트 추가)
- Tests: N/A (수동 실행 검증 필요)
